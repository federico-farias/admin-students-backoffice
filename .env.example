# Configuración del entorno - Copia este archivo como .env y ajusta los valores

# Usa 'true' para usar datos mock, 'false' para usar API real
VITE_USE_MOCK=true

# URL del backend (solo se usa cuando VITE_USE_MOCK=false)
# Asegúrate de que coincida con la configuración del proxy en vite.config.ts
VITE_API_BASE_URL=http://localhost:3000
